import { TranslateValidatorErrorFilter } from './translate-validator-error.filter';

describe('TranslateValidatorErrorFilter', () => {
  it('should be defined', () => {
    expect(new TranslateValidatorErrorFilter()).toBeDefined();
  });
});
